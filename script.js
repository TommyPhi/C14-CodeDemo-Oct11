// for loop example function
let forLoop = () => {
    let array=['a', 'b', 'c', 'd']
    for (let i=0; i<array.length; i++) {
        console.log(array[i])
    }
}

// Build your own loop under here
let byol = () => {
    
}
